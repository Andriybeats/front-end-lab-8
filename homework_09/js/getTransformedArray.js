function func(elem) {
  return elem + 1;
}
function getTransformedArray(array, func) {
  var arrayRes = [];  
	  arr.forEach(function(array) {
	    arrayRes.push(fun(array));
	  });
  return arrayRes;
}
